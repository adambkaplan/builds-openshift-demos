FROM registry.access.redhat.com/ubi8/ubi:latest
RUN rm -rf /etc/rhsm-host
RUN yum -y --enablerepo=codeready-builder-for-rhel-9-x86_64-rpms install \ 
    nss_wrapper \
    uid_wrapper && \
    yum clean all -y
RUN ln -s /run/secrets/rhsm /etc/rhsm-host
